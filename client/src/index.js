import React from 'react';
import ReactDOM from 'react-dom';
import GraphQL from './graphql'
import App from './App';
const el = document.getElementById("root");
ReactDOM.render(<GraphQL><App /></GraphQL>, el);
